<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GetJob.App.Views.Pages.LoginPage"
             Title="Login"
             BackgroundColor="#F3F4F6">

    <ScrollView>
        <VerticalStackLayout Padding="25" Spacing="30" VerticalOptions="CenterAndExpand">

            <!-- App Title/Logo -->
            <Label Text="Welcome Back!"
                   FontSize="32"
                   FontAttributes="Bold"
                   TextColor="#1F2937"
                   HorizontalOptions="Center" />

            <!-- Subtitle -->
            <Label Text="Sign in to find your next great opportunity."
                   FontSize="16"
                   TextColor="#6B7280"
                   HorizontalOptions="Center"
                   Margin="0,-10,0,0" />

            <!-- Login Form -->
            <VerticalStackLayout Spacing="15">
                <Frame Style="{StaticResource InputFrameStyle}">
                    <Entry Placeholder="Email or Username"
                           PlaceholderColor="#9CA3AF"
                           FontSize="16"
                           TextColor="#1F2937" />
                </Frame>

                <Frame Style="{StaticResource InputFrameStyle}">
                    <Entry Placeholder="Password"
                           PlaceholderColor="#9CA3AF"
                           IsPassword="True"
                           FontSize="16"
                           TextColor="#1F2937" />
                </Frame>

                <Label Text="Forgot Password?"
                       FontSize="14"
                       TextColor="#3B82F6"
                       HorizontalOptions="End"
                       TextDecorations="Underline">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Tapped="TapGestureRecognizer_Tapped" />
                    </Label.GestureRecognizers>
                </Label>

                <Button Text="Login"
                        Style="{StaticResource LoginButtonStyle}" />
            </VerticalStackLayout>

            <!-- Register Option -->
            <HorizontalStackLayout HorizontalOptions="Center" Spacing="5" Margin="0,20,0,0">
                <Label Text="Don't have an account?"
                       FontSize="14"
                       TextColor="#6B7280" />
                <Label Text="Register"
                       FontSize="14"
                       TextColor="#3B82F6"
                       FontAttributes="Bold"
                       TextDecorations="Underline">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Tapped="TapGestureRecognizer_Tapped_1" />
                    </Label.GestureRecognizers>
                </Label>
            </HorizontalStackLayout>
        </VerticalStackLayout>
    </ScrollView>

    <!-- Page Resource Dictionary for Styles -->
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="InputFrameStyle" TargetType="Frame">
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="Padding" Value="5" />
                <Setter Property="BackgroundColor" Value="#FFFFFF" />
                <Setter Property="BorderColor" Value="#D1D5DB" />
                <Setter Property="HasShadow" Value="False" />
            </Style>
            <Style x:Key="LoginButtonStyle" TargetType="Button">
                <Setter Property="BackgroundColor" Value="#3B82F6" />
                <Setter Property="TextColor" Value="White" />
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="FontSize" Value="18" />
                <Setter Property="Padding" Value="20,15" />
                <Setter Property="Margin" Value="0,10,0,0" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

</ContentPage>
